/**
 * Transform html elements to apply a consistent padding & margin to them.
 *
 * @param template
 * @return string
 */
export default (template) => {
  /**
   * Rules
   *  - move P padding and margin and create a table wrapping the content
   */


  return template;
};
